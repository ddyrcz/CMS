<Window x:Class="CMS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow"
        Width="525"
        Height="350">

    <Window.Resources>

        <Style x:Key="ControlBaseStyle" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Margin" Value="10,10,10,10" />
        </Style>

    </Window.Resources>

    <Grid Background="#FF54C8E9">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width=".8*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*" />
            <RowDefinition Height=".5*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  LOGO  -->
        <Image Style="{StaticResource ControlBaseStyle}" />

        <!--  MODE SELECTOR  -->
        <ListBox Grid.RowSpan="2"
                 Grid.Column="1"
                 ItemsSource="{Binding Modes}"
                 SelectedItem="{Binding SelectedMode}"
                 Style="{StaticResource ControlBaseStyle}" />

        <!--  CARS  -->
        <DataGrid Grid.RowSpan="3"
                 Grid.Column="2"                 
                 ItemsSource="{Binding Trucks}"
                 Style="{StaticResource ControlBaseStyle}" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Brand" Binding="{Binding Path=Brand}" Width="1*"/>
                <DataGridTextColumn Header="Registration number" Binding="{Binding Path=RegistrationNumber}" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--  MODE  -->
        <StackPanel Grid.Row="1" Style="{StaticResource ControlBaseStyle}">
            <TextBlock Text="Mode" />
            <Border Background="White"
                    BorderBrush="#A96FC8"
                    BorderThickness="3"
                    CornerRadius="5">
                <TextBlock HorizontalAlignment="Center"
                           FontSize="24"                           
                           FontStyle="Italic"
                           FontWeight="Bold"
                           Text="{Binding SelectedMode}" />
            </Border>
        </StackPanel>
        
        <!--  DETAIL VIEW  -->
        <Grid Grid.Row="2"
              Grid.ColumnSpan="2"
              Background="White"
              Style="{StaticResource ControlBaseStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1.8*" />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock FontSize="18"
                       FontWeight="Bold"
                       Text="Brand" />

            <TextBlock Grid.Row="1" Text="Registration number" />
            <Image Grid.Row="1"
                   Grid.RowSpan="7"
                   Grid.Column="1" />
            <TextBlock Grid.Row="2" Text="VIN number" />
            <TextBlock Grid.Row="3" Text="Term technical research" />
            <TextBlock Grid.Row="4" Text="Tacho legalization" />
            <TextBlock Grid.Row="5" Text="List UDT" />
            <TextBlock Grid.Row="6" Text="OC policy" />
            <TextBlock Grid.Row="7" Text="AC policy" />

        </Grid>

    </Grid>
</Window>
