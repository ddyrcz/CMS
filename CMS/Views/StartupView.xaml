<Window x:Class="CMS.Views.StartupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viemodel="clr-namespace:CMS.ViewModels"
        Title="StartupView" Height="300" Width="300">
    <Window.DataContext>
        <viemodel:StartupViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <Button Content="Lista"  />
        <Button Grid.Row="1" Content="Raport" />
    </Grid>
</Window>
